# Copy to .env and fill in. .env is gitignored; never commit a real key.
# Free key from https://console.groq.com/keys
GROQ_API_KEY=

# Python backend: allow browser requests from the deployed Vercel site.
# FRONTEND_ORIGIN=https://your-app.vercel.app

# Vite frontend: direct production API requests to the Render backend.
# VITE_API_BASE_URL=https://your-atlas-api.onrender.com
